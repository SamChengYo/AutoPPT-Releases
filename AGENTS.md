# 公開版本庫維護指引

本倉庫 `SamChengYo/AutoPPT-Releases` 專門提供公開下載與版本紀錄。應用程式原始碼保存在獨立的私人 `SamChengYo/AutoPPT` 倉庫。

- 開始發布前，先閱讀私人原始碼倉庫的 `docs/releasing.md`；其中記錄完整建置、驗證、發布及更新主機移轉流程。
- 維持兩個倉庫各自的 Git 歷史。不要將私人原始碼庫 mirror、merge、cherry-pick 或整包推送到本倉庫。
- Git 檔案只放下載說明、維護指引及 `versions/vX.Y.Z.md` 等公開版本資料。安裝檔上傳至 GitHub Release，不提交大型二進位檔到 Git。
- 每個正式 Release 使用 `vX.Y.Z` tag，附上同一建置的 `.exe`、`.exe.blockmap`、`.exe.sha256` 與 `latest.yml`；四個附件完成上傳且驗證後才發布。
- 已發布 tag 與附件保持不變；修正使用新版本號。
- 不公開使用者專案、API Key、Token、加密設定備份、內部日誌或私人原始碼壓縮檔。
- 發布後確認匿名下載、檔案雜湊及應用程式實際更新檢查。不要把只有建置成功或建立 draft 當作已發布。
- 預設更新來源目前是此公開倉庫。未來更換主機須依使用者指定的網址處理，並保留既有使用者的更新路徑。

單純修改文件不需要提高應用程式版本或重新發布安裝檔。
